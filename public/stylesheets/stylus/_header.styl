.nav-bar
	background-color $color1
	
.carousel {
  &:after{
    content: "";
    diplay: block;
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(45deg,  rgba(194,23,140,.6) 0%, rgba(56,193,217,.6) 100%);
  }
}